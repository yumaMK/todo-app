export type Todos = {
	id: number;
	title: string;
	status: string;
	summary: string;
	limit: string;
}

export type AddTodo = {
	id: number;
	title: string;
	status: string;
	summary: string;
	limit: string;
}
